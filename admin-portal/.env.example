BRIDGE_BASE_URL=http://localhost:8081
BRIDGE_API_KEY=replace_me

# Backend (whatsapp-bridge) must also be configured with:
# ADMIN_PASSWORD_HASH=<bcrypt hash of your admin password>
# ADMIN_SESSION_SECRET=<random string>
